import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  ul: {
    justifyContent: 'space-around',
    borderRadius: 15,
    color: 'inherit',
    position: 'static',
  },

}));
